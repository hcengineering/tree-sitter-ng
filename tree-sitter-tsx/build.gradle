tasks.named('downloadSource') {
    srcLibName = "tree-sitter-typescript"
    url = "https://github.com/tree-sitter/tree-sitter-typescript/archive/refs/tags/v${version}.zip"
}

tasks.named('buildNative') {
    srcLibName = "tree-sitter-typescript"
    subDirectory = "tsx"
}
